# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

_target
# Exclude binaries and executables these are built by the build process
src-tauri/resources/*.exe

# Packaged release output
dist-release

# The detectable-games list is ~12 MB and is fetched by CI at build time.
# Keeping it out of the repo (it never was tracked) also stops it from being
# committed by accident.
src/assets/gamelist.json